import 'package:flutter/material.dart';

// Constants for the WillOpus List app.

const Color kBaseAppColor = Color(0xFF47a239);
const String kDefaultListColorHex = 'FFFFFFFF';

const String kFirebaseUrl = 'willopus-lists-default-rtdb.firebaseio.com';
const String kTestFile = 'TestList/test';
//const String kTestFileItem = 'TestList/test/@ID.json';

const String kMasterIDKey = 'master_id';

// TODO: NOTE: This uses auto-generated localizations that in turn become strings 
// embedded in the app. For smaller apps with only a few translated languages, this 
// is fine, and very fast. If later we have more languages and/or more translated 
// strings, we may move to loading each language seperately as it's own file.
