Will Opus Lists TODO:
========================

Rewrite models & setup:
    1) List Item Object:
        - title
        - description
        - * isDone... !!!rename variable as isCompleted, is Completed?
            - is for HAS ITEM BEEN CLICKED DONE! (is checkbox clicked!)
            - can be active but checked... not yet deleted.
                - in UI determines item's background color, and whether a "Are you sure" popup dialog appears before deleting

        - id - assigned by server (or internal saving setup with a uuid)
            - used as they key to the individual object
            - may not need to be saved in list item server object, since it is used to find it?

        - UI has a checkmark for displaying ready (pale) or done (dark)
            - this determines saving... saving when ready pops up dialog (sure?) Done just deletes object?
        
            - add future item sync icon for imediate item update, or adapt save setup?
            - Especially when future multiuser sync?
                - things to figure out....

    2) List Object:
        - list title
        - list description
        - list color?
    
        - list of list item keys
            - order of keys determines list order
                - re-sorting an item resorts & saved the list object
        
        - id - assigned by server (or internal saving setup with a uuid)
            - used as they key to the individual object
            - saved (for now) in a global var, or for internal (no server) storage, can be hard-coded
                - need to use uuid or server key once handling multipl lists in a master object
    
    3) Master Object (later) - WillOpus-Lists-Root? WillOpus-Lists-Master? Core? Naming??!!
        - list of list objects, ordered by key
        - will need to setup UI screen!
    
    4) Grabbing a List Object, from its key:
        - grab list object from key, async wait....
        - for each item key in list's key list:
            - grab item object for key, internally associating it with the key if needed

Future Online Multi-User Server Stuff!
    - handle when off-line
        - handle when to sync changes (buttons to sync, pop-up dialogs when conflicts?)
        - handle simple version control if dual users update an object or list between syncs?



================================

Data Setup:
    'master_key': string - key for the master object
    ...
    all other objects are found through the keys stored with the master object as Root

================================

Next Steps:

1) setup master list screen
    - master_services:
        - set_master_id (sets master id as 'master_id': object key)
        - update_master_list
    - setup a temp test object for master object, with a single empty list & title 'WillOpus Test List'
    - LATER: setup adding new lists
        - top right add icon
        - popup setup dialog (title, list color)

    UI: simple list display, showing each tile: list title and set bkg color

2) Go to current list screen
    - list services, update!

    - UI should mostly work as is....

From email:
Services:
   master:
       - save/get master object id key
           - saves the master object id under the local key "master_key"
           - get the key for object grabbing

       - save/get master object
           - save (in: willopus-master-list)
               - saves object to user prefs
                   - returns id key for the object
                       - expects the caller to then save the id in the object
                           - for future Firebase setup that returns the id key after save....

   list:
       - get/save list object
           - on save return id key (see above Firebase note).

=========================================

NOT FOR LIST APP! For my other Google Maps golf app....
Future golf course data? Currently planning a simple hard-coded set for Los Altos 9 hole....
    - store a set of small data files (XML or similar) with the data for each golf course available
    - stored on-line
    - also, online master list of all the available courses & their current on-line links to each course data

    - One place online to store all the data, which can be constantly add to & updated.
        - each launch loads the master list of courses, which determines the course selection screen.
        - download from server each course's data upon selection.

List app data stored on Firebase:
    NOTE: This is just base obj access, not all the user authentication Firebase needs after an open 30 day trial....
    Encampsolate this & similar code into a helper funtion:

    if (kUseOnlineServices) {
      var uri = Uri.https(
        kFirebaseUrl,
        '$kTestFile.json',
      );

      try {
        final response = await http.post(
          uri,
          headers: {'Content-Type': 'application/json'},
          body: json.encode(item.toJson()),
        );
        debugPrint('response status code = ${response.statusCode}');
        return response.statusCode >= 200 && response.statusCode < 300;
      } catch (err) {
        return false;
      }

    File: online_firebase_helper.dart
    class OnlineFirebaseHelper
        - static func add Firebase object (in: json body), returns/sets returned Firebase key ID
        - static func patch Firebase object (in: obj key, json body)
        - static func delete Firebase object (in: obj key)

        - return appropriate data, and throw appropriate errors!

