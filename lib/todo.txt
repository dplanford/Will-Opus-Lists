Will Opus Lists TODO:
========================

NOT FOR LIST APP! For my other Flutter Google Maps golf app....
Future golf course data? Currently planning a simple hard-coded set for Los Altos 9 hole....
    - store a set of small data files (XML or similar) with the data for each golf course available
    - stored on-line
    - also, online master list of all the available courses & their current on-line links to each course data

    - One place online to store all the data, which can be constantly add to & updated.
        - each launch loads the master list of courses, which determines the course selection screen.
        - download from server each course's data upon selection.

============================================

Updated README.md (once I figure out the Markdown formatting ;-)

<App details, blah, blah>

lib/assets/
    - any assets (icons, images, sound files, etc.) that are local to the app's data.
    - depending on the app setup, this directory might move (see the pubspec.yaml file for the app's specific location)

lib/l10n/
    - Language localization files. This includes one ARB file for each language.
    - AppLocalizations.dart - this is an auto-generated code file (along with other associated files), built when getting Flutter packages.

lib/model/
    - core data objects used by this app. While some may be purely for local app use, many are defined to be JSON encoded/decoded for storage & recovery.

lib/helper/
    - various helper classes with useful functionality. Some are simple wrappers around general Flutter capabilities (such as the tiny SnackBar helper). Others are more specific to app functionality.

lib/bloc/
    - if the app uses Flutter BLoC event/state streaming of storage calls, this is where the calls, event & state classes, and bloc repositories would be defined.

lib/services/
    - the low level data storage calls, and all core low-level server side API calls.
        - for this app, most service calls are defined to default to local storage (using the app's local user-prefs)
        - however, most calls include an override flag to try to send the data object to a Firebase cloud setup
            - this cloud setup is not yet set up! But the core code exists as an example, and for future work.

lib/menu/
    - classes & Flutter widgets dedicated for menu items & selection dialogs.

lib/widgets/
    - useful Flutter widgets... this includes simple things like a widget that switches between an iOS/Android styled wait spinner depending on run platform, to app specific list tiles.

lib/screens/
    - the full screens used by the app. All should be based on a Scaffold screen widget, usually with an AppBar.

lib/
    - main.dart
        - the root start of the app, kept simple.
    - constants.dart
        - globally constant values, such as Firebase server URL hooks, and default app color values.

Project Root:
    - pubspec.yaml
        - the core Flutter build setup, defining included external packages, assets directories, and testing hooks.

=============================

Next app steps....

1) Get list screen working again
    - display with reordable tiles
    - redo the reorderable setup
    - test/fix all list/item services

2) Setup reordable master list

3) Small thing... fix the color dialog leaving the keyboard out focus bug.

4) Maybe get to images....
    First Pass (local storage only):
        - just store local link to the image in the user's local phone
        - also could pick an image URL from a browser window?
