Will Opus Lists TODO:
========================

NOT FOR LIST APP! For my other Flutter Google Maps golf app....
Future golf course data? Currently planning a simple hard-coded set for Los Altos 9 hole....
    - store a set of small data files (XML or similar) with the data for each golf course available
    - stored on-line
    - also, online master list of all the available courses & their current on-line links to each course data

    - One place online to store all the data, which can be constantly add to & updated.
        - each launch loads the master list of courses, which determines the course selection screen.
        - download from server each course's data upon selection.

==========================
Additions to master helper:

Get Master List:
    - grab master key from storage (local key is 'master_ID', to get the stored master key as it's data).
        - if return null, then need to create a brand new master list (first app launch, no data storage yet)
    - if returned key is non-null string, use it to grab the master list object
        - if this returns null, then something went wrong... note error, and then create new master list object.

    - this should be baked in to the access of the master screen init.

    - UI on error:
        - launch master list screen
        - on no master key stored, pop-up a dialog:
            - first time? create a new master list? OK!
        - on master key exists, but does not return an object:
            - pop-up error... note that we must create a new master list... OK?
        - if a master list object is returned, then use it, update it, etc....

On all screens, fetching the screen's data should start with an object key ID...
    - master screen ... grab master key from local storage, load it's data
    - list screen ... input: key for the list object, and grab all data for it & sub item objects from services
    - item detals screen ... input: key for the item object, and only update/delete this item from it's key

========================================

BIG NEW TODO (for future Firebase cloud storage?):
What if the user delete's his app, has to start over, but on-line cloud data still exists... 
    - can I store the master-key-id also on the user's Firebase cloud in a way it can be globally accessed without an object key... just for the authenticated user? a recovery key to the on-line stored master key, which could restore the entire list database for the user....

Master screen list cards (1st pass):
    - top bar is a simple color header bar, displaying the hex stored color of the list.
    - then largish string title of the list.
        - possibly add a smaller description string option to add/display?
        - thi would also need UI textfield/textarea additions.

    - ALL wrapped in a tappable container with an onPressed func call like a button.
        - could be an adapted TextButton?

Color selection popup change against core popup dialog?
    - build it's widget with a semi-transparent border/margin?
    - it pops up the same size as the base list create popup, and overlays it, so displey it with some transpancy against the source popup.
